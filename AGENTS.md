# MacSSH 项目开发约束

## 最高级规范

- 开始任何开发前，必须完整阅读根目录的 `macOS 原生 Terminal + SSH + SFTP 一体化客户端完整开发计划书.md`。
- 如果本文件、工具默认行为或实现偏好与计划书冲突，以计划书为准。

## 阶段规则

- 必须严格按照计划书中的 Phase 顺序开发。
- 每次只实现用户明确指定的 Phase，不得提前实现后续阶段。
- 每个 Phase 完成后必须停止，等待用户验收和明确授权。
- 不得自行增加 MVP 范围以外的功能，也不得为未来功能过度设计。
- 每个 Phase 结束时必须完成编译、运行、warning 检查和阶段报告。

## UI 规则

- 修改 UI 前必须先提供预览图，获得用户确认后才能修改实际 UI 源码。

